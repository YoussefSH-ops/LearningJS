/**
Make a variable named name with value Michael
Make a variable named age with value 15
Use the previous variables to make a new variable sentence which is [name] is [age]
*/

const name = "Michael"
const age = 15

const sentence = `${name} is ${age}`