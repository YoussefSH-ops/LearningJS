// Functions
// a function is a set of instructions you can invoke and give it data

function add (x , y){
    return x + y
}
const results = add(2 , 3)
console.log(results)
const results1 = add(5 , 8)
console.log(results1)

// add is the name
// (x , y) are the paramaters
// (2 , 3) are the arguments
// when you put () on a function is when you invoke it

function makeSentence (name , age , displayAge){

}
